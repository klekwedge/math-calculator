"use strict";

const operationButtons = document.querySelectorAll("[data-operation]");
console.log(operationButtons);


const valueButtons = document.querySelectorAll("[data-value]");
console.log(valueButtons);